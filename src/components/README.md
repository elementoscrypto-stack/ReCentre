# Components

Use this folder for future extraction of V233 components such as:
- Background
- Sidebar
- MobileBottomNav
- Panel
- Button
- PageHero
- CommandPalette
- ToastCenter

The current bundle keeps them inside `App.jsx` to preserve the original working look.
